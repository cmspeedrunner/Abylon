fun main(){
    // We can say hello world like this:
    var hello = "Hello"
    var world = ", World!"
    write hello + world

    // Or you can just do this!
    var greeting = hello + world
    write greeting

    // Or, we can do all this:
    
    var hello = "Hello, "
    var greeting = hello  + "World!" 
    write greeting

    var world = "World!"
    var greeting = "Hello, "+ world 
    write greeting
}